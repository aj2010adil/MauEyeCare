<UserControl x:Class="MauEyeCare.Desktop.Views.ReleaseNotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:MauEyeCare.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Release Notes" 
                       Style="{StaticResource MaterialDesignHeadline3TextBlock}"
                       Foreground="{DynamicResource PrimaryHueMidBrush}"
                       FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <materialDesign:Chip Content="{Binding Version}" 
                                     Background="{DynamicResource SecondaryHueMidBrush}"
                                     Foreground="White"
                                     Margin="0,0,10,0"/>
                <TextBlock Text="{Binding LastUpdate, StringFormat='Updated on {0}'}" 
                           Style="{StaticResource MaterialDesignBody1TextBlock}"
                           VerticalAlignment="Center"
                           Opacity="0.7"/>
            </StackPanel>
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Changes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="0,0,0,25" UniformCornerRadius="12" Padding="25">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" 
                                           Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,15"/>
                                
                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,5,0,5">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <materialDesign:PackIcon Kind="CheckCircle" 
                                                                         Foreground="{DynamicResource PrimaryHueMidBrush}"
                                                                         VerticalAlignment="Top" 
                                                                         Margin="0,3,12,0"/>
                                                <TextBlock Grid.Column="1" 
                                                           Text="{Binding}" 
                                                           Style="{StaticResource MaterialDesignBody1TextBlock}" 
                                                           TextWrapping="Wrap"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
